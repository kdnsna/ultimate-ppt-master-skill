"""Kimi public-editor adapter (opt-in export/QA backend).

Everything specific to Kimi lives here: URLs, iframe RPC, DOM selectors, CDP
target hints and the download flow. The rest of UPM only sees the exporter /
renderer interfaces.
"""

