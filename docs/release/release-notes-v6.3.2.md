# Ultimate PPT Master v6.3.2 — visual correction and security foundation

> **Unreleased candidate.** This file defines a source-level milestone boundary. Its presence on `main` or Pages is not evidence of a dedicated v6.3.2 tag, GitHub Release, or marketplace publication. Do not cite this document as public-release evidence.

[Read the primary Chinese candidate notes](../zh-CN/release/release-notes-v6.3.2.md)

## Plain-Language Update Notes

- The default workspace and proof gallery move from heavy black surfaces to a warm-paper system with evidence-blue actions.
- Required containers use a restrained 12/18/24px radius scale; charts, tables, and evidence rules remain precise rather than being rounded indiscriminately.
- Automatically selected formal-office covers stay light unless the user or a real brand direction explicitly requests a dark stage.
- Browser-origin, JSON content-type, request-size, project-path, symlink, and manifest-authenticity checks become explicit Bridge boundaries.
- Legacy visual-direction identifiers migrate into the six registered v6 contracts; arbitrary direction identifiers are rejected instead of entering production metadata.

## Candidate Boundary

This slice owns the Warm Paper UI tokens and public visual surfaces, the v6 direction registry/migration, and the Bridge request/path/HMAC foundation. It does not own later source truthfulness, session delivery, refinement, or release-candidate documentation.

## Required Evidence Before Publication

- desktop/mobile visual review at 1440px and 390px;
- illegal Origin, wrong Content-Type, oversized request, traversal, symlink, and tampered-manifest tests;
- Web and Desktop builds with the public version markers still aligned;
- no claim that pending PowerPoint/WPS/LibreOffice review has passed.

## Independent Rollback Boundary

Revert the Warm Paper token/surface changes, registered-direction migration, and Bridge request/path/HMAC guard group together. Keep `deck-session-v6`, DeckIR 1.0, and v6.3.1 artifact contracts unchanged. This rollback must not remove later user projects or rewrite unsigned handoffs into trusted projects.

Treat this as a logical rollback boundary. A versioned release should preserve it through a dedicated reviewable commit or a documented revert map so a v6.3.2 rollback does not require reverting v6.3.3–v6.3.6; this note does not attest that a dedicated release artifact exists.
