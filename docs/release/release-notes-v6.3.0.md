# Ultimate PPT Master v6.3.0 — default quality and public proof

> **Unreleased draft.** This file defines the intended, independently reversible v6.3.0 boundary. The tag and GitHub Release may be created only after the milestone commit is merged and CI passes.

The planned v6.3.0 milestone adds default-quality contracts and two public proofs on top of the v6.2 loop:

- handoffs record `bestEffectBrief`, `promptQuality`, `recommendedRoute`, `decisionReason`, and route source;
- Web, Bridge, and Python share one 52-case Best-Effect parity fixture;
- three formal-office scenarios use deterministic role/recipe mappings and a no-three-identical-layout gate;
- a nine-slide synthetic executive-review PPTX ships with native-object, PPTLint, and quality reports;
- the GPT-5.6 “Three Orbits” nine-slide Web Deck ships with a 390px mobile proof and primary sources.

Rollback boundary: public proof, routing metadata, and direction contracts can be reverted together without changing the v6.2 local artifact API.
